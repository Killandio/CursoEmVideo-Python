# This program reads a real number from the user and displays only its integer part.

numero = float(input("Digite um número real: "))
parteInteira = round(numero,)

print(f"A parte inteira de {numero} é {parteInteira}.")
